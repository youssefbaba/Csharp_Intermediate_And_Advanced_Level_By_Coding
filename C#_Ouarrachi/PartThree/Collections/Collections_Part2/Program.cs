namespace Collections_Part2
{
    public class Program
    {
        /*
         - Array and ArrayList : both of them are going to store the values by using Key/Value combination but Key is pre-defined index(int) which is start from 0 to (number of items - 1).
         - Hashtable : stores the values by using Key/Value combination but Key is user-defined of type object.

         */
    }
}