namespace Collections_Part5
{
    public class Program
    {
        /*
                  
         - ArrayList and List<TValue> : both of them are going to store the values by using Key/Value combination but Key is pre-defined index(int) which is start from 0 to (number of items - 1).
         - Hashtable and Dictionary<TKay,TValue> : stores the values by using Key/Value combination but Key is user-defined.
         - Non-Generic Collection                   - Generic Collection
            - ArrayList                                - List<TValue>
            - Hashtable                                - Dictionary<TKay,TValue> 
         - Tkey in dictionary must be unique.
         - Dictionary stores values in sequence but Hashtable not in sequence .



         */
    }
}